Задание4 4-ий пункт

#include <iostream>
using namespace std;
 
int main ()
{
    setlocale( LC_ALL,"Russian" );
        
    float a,b,c;
 
    cout << "Введите три стороны треугольника: ";
    cin >> a >> b >> c;
 
    if ((a*a==b*b+c*c) || (b*b==a*a+c*c) || (c*c==a*a+b*b)) std::cout << "Треугольник прямоугольный.\n";
    else cout << "Треугольник не является прямоугольным.\n";
    return 0;
}

	Проверки, где a, b, c соотв:
	1. (3, 4, 5) ответ: Треугольник прямоугольный
	2. (12, 47, 58) ответ: Треугольник не является прямоугольным
        3. (8, 6, 2) ответ: Треугольник не является прямоугольным
